<%- include('includes/head.ejs') %>

</head>

<body>
        <%- include('includes/nav.ejs') %>

    <main>
        <h1>My Products</h1>
        <p>List of all the products...</p>

        <% if (prods.length > 0) { %>
            <% for (let product of prods) { %>
                <h1><%= product.title %></h1>
                <p>I'm sure that product was here somewhere...</p>
            <% } %>
        <% } else { %>
            <h1>No Products Found!</h1>
        <% } %>
    </main>
    <%- include('includes/end.ejs') %>